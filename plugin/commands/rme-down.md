---
description: Derruba o docker-compose do RME Agent Bridge
---

Rode `docker compose -f docker/docker-compose.yml down` a partir da raiz do
repo `rme-agent-bridge`. Confirme com o usuário se ele quer `-v` (apaga
volumes) — normalmente não precisa.
